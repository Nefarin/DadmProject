<UserControl x:Class="EKG_Project.GUI.VisualisationPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EKG_Project.GUI"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="300">
    <Grid>
        <!--
        <DataGrid Name="headerTable" AutoGenerateColumns="False" AlternatingRowBackground="#c2ccdb">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Fs}" Header="Fs" Width="80"/>
                <DataGridTextColumn Binding="{Binding Samples}" Header="Number of samples" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
        !-->
        <TabControl Name="EcgDynamicTab" ItemsSource="{Binding}" TabStripPlacement="Left" Margin="0,50,0,0"/>
    </Grid>
</UserControl>
