<UserControl x:Class="EKG_Project.GUI.AnalysisControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EKG_Project.GUI"
             mc:Ignorable="d">
    <Grid DockPanel.Dock="Top" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <local:ModulePanel Grid.Column="0" Grid.Row="0" Margin="0,0,0,0"/>
        <WrapPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <Button>
                <StackPanel Orientation="Horizontal">
                    <Image Source="images/play.png"/>
                    <Label Padding="0">Play</Label>
                </StackPanel>
            </Button>
            <Button>
                <StackPanel Orientation="Horizontal">
                    <Image Source="images/pdf.png"/>
                    <Label Padding="0">Get pdf</Label>
                </StackPanel>
            </Button>
            <Button x:Name="exitButton" Click="exitButton_Click">
                <StackPanel Orientation="Horizontal">
                    <Image Source="images/close.png"/>
                    <Label Padding="0">Close</Label>
                </StackPanel>
            </Button>
        </WrapPanel>
    </Grid>
</UserControl>
